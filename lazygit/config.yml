# lazygit configuration.
#
# NOTE: lazygit does NOT use git's `core.pager`, so delta has to be configured
# here separately. delta still picks up styling (navigate, dark, side-by-side,
# etc.) from the [delta] section of your ~/.gitconfig automatically.
git:
  paging:
    colorArg: always
    pager: delta --paging=never
